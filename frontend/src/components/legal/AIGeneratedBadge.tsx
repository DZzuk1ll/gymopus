import { Badge } from "@/components/ui/badge";

export function AIGeneratedBadge() {
  return (
    <Badge variant="secondary" className="text-xs font-normal bg-primary/[0.08] text-primary border-primary/10">
      AI 生成
    </Badge>
  );
}
